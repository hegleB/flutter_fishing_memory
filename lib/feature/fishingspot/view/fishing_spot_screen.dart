import 'package:flutter/material.dart';

class FishingSpotScreen extends StatelessWidget {
  const FishingSpotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("낚시터 찜")),
    );
  }
}